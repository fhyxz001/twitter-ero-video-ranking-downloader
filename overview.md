# 极简 UI 设计方案 — 总结

## 变更概述
对 Twitter 视频下载器的三个页面（仪表盘、海报墙、瀑布流）进行了全面的极简化重新设计。

## 设计系统 CSS（replaced: static/css/design-system.css）

- **470 行**（之前 481 行），风格从暗色 "Copper Noir" 改为浅色 "Paper"
- 设计理念：如纸张般轻盈、克制、专注。UI 应褪入背景，让内容说话
- 移除 Google Fonts 外部依赖，改用系统字体栈，零布局偏移
- 配色：暖白底 (#fafaf9)，石灰色调文字，钢灰色点缀，清晰但不刺眼
- 圆角仅 4-8px，只用细线边框（无阴影），拒绝 AI 审美：无渐变、无发光、无模糊、无霓虹色
- 5 级字体大小，通过字重实现层级区分
- 4px 基础间距系统，统一的标记体系
- 卡片组件改为 "section"：减少不必要的嵌套，用留白和边距自然分组
- 保留了所有功能状态：loading、empty、error、disabled、hover、active

## 页面改动

### 仪表盘 (templates/index.html)
- 顶部导航栏：移除渐变 logo，纯文字品牌名 + 简洁 tab 切换
- 状态条：更小更轻，扁平化
- 博主管理区：缩小的头像和更紧凑的列表
- 设置抽屉：更清晰的 section 分割，简化表单
- 日志面板：深色终端风格保留，更小的字号
- 所有交互和 API 调用保持不变

### 海报墙 (templates/poster.html)
- 更简洁的 hero 区，统计数字更突出
- 简化工具栏：使用分隔线替代卡片背景
- 视频卡片：更小圆角，更轻边框，hover 时仅边框变色
- 保留全部功能：筛选、排序、分页、选中、批量删除、封面替换
- 播放器模态框：移除毛玻璃效果，纯白色简洁外观

### 瀑布流 (templates/waterfall.html)
- 排行榜切换按钮：选中态用实色填充（之前是铜色，现在是石板灰）
- 设置弹窗：缩小对话框尺寸，居中样式
- 视频卡片：CSS 多列瀑布流保持不变，仅样式轻量化
- 全部原有交互逻辑完整保留

## 设计决策记录

| 决策 | 理由 |
|------|------|
| 浅色主题替代暗色主题 | 暗色 UI 已成为 AI 生成代码的标志性审美，浅色工具界面更成熟可信 |
| 系统字体栈替代 Google Fonts | 零网络依赖，即时渲染，100% 无布局偏移 |
| 移除所有阴影 | 阴影是装饰性元素，扁平边框已经足够表达层级 |
| 仅保留一种强调色 | 1-2 色＋中性色符合极简原则，减少视觉噪音 |
| 保留所有功能 | 简化的是视觉，不是功能——所有原有操作路径完整保留 |
