# 2026-07-14

## Bug 修复：设置反显 & 保存无响应
- 根因：`configStore.load()` 从未在初始化时调用，页面刷新后 `configStore.config` 始终为 null
- 修复：在 HomeView.onMounted 中添加 `await configStore.load()`
- 影响：
  - 下载根目录反显问题 ✅
  - 设置抽屉 HTTP 代理反显问题 ✅
  - 设置抽屉保存无响应问题 ✅

## 移除浏览按钮
- HomeView.vue 中移除了 `browseDirectory` 函数和模板中的"浏览"按钮

## 彻底移除博主管理、Twitter 设置、自动爬取模块
- **后端 main.py**：删除全部 Twitter 博主相关代码（约 500 行）
  - 配置默认值、验证逻辑、缓存函数、爬取函数、API 路由
  - 清理了 unused imports（pickle, asyncio, httpx）
- **前端**：
  - 删除 `BloggerList.vue`、`stores/blogger.ts`
  - `SettingsDrawer.vue`：移除 Twitter Cookie、博主爬取设置 UI
  - `HomeView.vue`：移除 BloggerList 组件、博主状态项、twitter payload 字段
  - `api/index.ts`：移除 Blogger 类型定义和 API 对象
  - `stores/status.ts`：移除 bloggerState
  - `styles/index.css`：移除 blogger-chip 样式
