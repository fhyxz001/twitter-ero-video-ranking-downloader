## 2026-07-09 第二轮完整 UI 重构（合并后）

### 变更原因
用户合并了外部代码，导致之前的设计系统被覆盖、模板被回退为旧版浅色内联样式。

### 完成内容

**设计系统** `static/css/design-system.css` — 完全重写
- "Copper Noir" 设计语言，深色主题 + 暖铜色强调 (#d97757)
- 完整的设计令牌（颜色、字体、间距、圆角、阴影、过渡）
- 组件库：按钮（4 变体）、表单、卡片、徽章、标签、博主列表、日志面板
- 布局：状态条、网格、瀑布流、顶部栏、模态框、Drawer、Player
- 骨架屏、空状态、分页器、页签、进度指示器
- 全部使用短类名（如 `.btn-pri` `.btn-gho` `.mcard`），减少 HTML 体积

**模板重写**（3 个页面全部重写）
- `templates/index.html`：适配简化后后端（无内容筛选模块），保留全部功能
- `templates/poster.html`：深色海报墙，保留预览/替换封面/批量删除/时长探测
- `templates/waterfall.html`：瀑布流，保留标签/分页/下载选中

### 后端适配
后端已合并简化：`/` 不再传递筛选选项，`/save` 简化字段，`/waterfall` 只传 per_page。模板已完全适配。

### 验证
- CSS: 200 ✅
- Index: 200 ✅
- Poster: 200 ✅
- Waterfall: 200 ✅

---

## 2026-07-09 第三轮极简化重构

### 用户要求
设计全新 UI，极简布局，不需要复杂元素。

### 完成内容

**CSS 设计系统** `static/css/design-system.css` — 从 "Copper Noir" 暗色→浅色 "Paper" 极简主题
- 移除 Google Fonts 依赖，改用系统字体栈
- 配色：暖白底 `#fafaf9` + 石灰色文字 + 钢灰强调色 `#4b5563`
- 移除：所有阴影、渐变、发光、毛玻璃、圆角 > 8px
- 组件名从 `.card` 改为 `.section`，减少嵌套层级
- 保留全部状态：hover/active/disabled/focus-visible/loading/empty/error

**三个模板全部重构**（templates/*.html）
- 保持原有 API 调用和 JS 逻辑不变
- 仅修改 HTML 结构和 class 名称适配新 CSS
- 简化视觉装饰，保留全部功能
